# Phase 5 — Exit Checklist

## Routes

- [x] `/` landing with CTA
- [x] `/plan` onboarding form
- [x] `/itinerary` results

## Components

- [x] `PlanForm`, `ItineraryTimeline`, `WarningsBanner`, `ItineraryMap`
- [x] `ApiStatusBadge`, `TripSummaryBar`, `CostDisclaimer`

## Quality

- [x] Dark theme, mobile-friendly
- [x] No `GROQ_API_KEY` in frontend
- [x] Vitest: validation + api client
- [x] `npm run build` passes
- [ ] Manual E2E walkthrough (landing → plan → itinerary)

---

**Gate status:** Complete (automated). Run manual E2E with `make dev`.
