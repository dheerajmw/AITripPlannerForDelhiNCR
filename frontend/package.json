{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000 --hostname 127.0.0.1",
    "dev:clean": "rm -rf .next && next dev --port 3000 --hostname 127.0.0.1",
    "recover": "rm -rf .next && next dev --port 3000 --hostname 127.0.0.1",
    "clean": "rm -rf .next",
    "prebuild": "node scripts/check-dev-not-running.mjs",
    "build": "rm -rf .next && next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vis.gl/react-google-maps": "^1.8.3",
    "leaflet": "^1.9.4",
    "lucide-react": "^0.469.0",
    "next": "15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-leaflet": "^5.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/leaflet": "^1.9.15",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.5.0",
    "eslint": "^9",
    "eslint-config-next": "15.1.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
